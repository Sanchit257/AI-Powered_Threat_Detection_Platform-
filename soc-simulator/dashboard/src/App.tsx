export default function App() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>SOC Simulator</h1>
      <p>React dashboard placeholder. API health: try <code>/api/health</code> via Nginx.</p>
    </main>
  );
}
